//this is a test file for saving and using the chatID across multiple documents, easier than typing out this value multiple times
//the return value is the unique identifier for the bot's test server (Bot Test Server, BTS) and makes the bot send any messages there when 
//  the "send" function is called. Ask someone to be added to the server if you want to test commands for the bot.
const getGeneralChatID = () =>{
    return "717073663324848141"
}
exports.getGeneralChatID = getGeneralChatID;